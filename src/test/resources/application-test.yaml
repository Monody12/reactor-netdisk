spring:
  sql:
    init:
      mode: always
      schema-locations: classpath:schema-h2.sql
  r2dbc:
    url: r2dbc:h2:mem:///testdb;MODE=MySQL
    username: sa
    password:

logging:
  level:
    root: WARN

file:
  upload:
    path: D:\\Temp

